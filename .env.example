# ================================================
# EasySSH 环境变量配置
# ================================================
#
# 📖 使用说明：
#
#   【Docker 部署】
#   - 配置已内置在 docker/docker-compose.yml 中
#   - 直接运行：cd docker && docker compose up -d
#   - 如需修改配置，请编辑 docker-compose.yml 文件
#
#   【开发环境】
#   1. 复制此文件：cp .env.example .env
#   2. 修改配置值（特别是标记 [必改] 的项）
#   3. 启动开发环境：./scripts/dev.sh
#
# 🔐 生成安全密钥：
#   JWT Secret:      openssl rand -base64 48
#   Encryption Key:  openssl rand -base64 24
#
# ⚠️  生产环境必须修改标记 [必改] 的配置项
# ================================================

# ================================
# 数据库配置 (PostgreSQL)
# ================================
# 主机地址（Docker: postgres | 开发: localhost）
DB_HOST=postgres
DB_PORT=5432
DB_USER=easyssh
DB_NAME=Easyssh_db

# 数据库密码 [必改]
# 生产环境请使用强密码（16+ 字符，包含大小写字母、数字、特殊字符）
DB_PASSWORD=easyssh_dev_password

# SSL 模式（disable | require | verify-ca | verify-full）
DB_SSLMODE=disable

# SQL 调试日志（生产环境建议 false）
DB_DEBUG=false

# ================================
# 缓存配置 (Redis)
# ================================
# 主机地址（Docker: redis | 开发: localhost）
REDIS_HOST=redis
REDIS_PORT=6379
REDIS_DB=0

# Redis 密码（留空表示无密码）
REDIS_PASSWORD=

# ================================
# 应用配置
# ================================
# 运行环境（development | production）
ENV=production

# Gin 框架模式（debug | release）
GIN_MODE=release

# 服务端口
PORT=8521
WEB_PORT=8520

# 后端服务地址（前端使用）
# 开发: http://localhost:8521
# 生产: http://your-domain.com/api 或 https://api.yourdomain.com
NEXT_PUBLIC_API_BASE=http://localhost:8521

# ================================
# 安全配置 [必改]
# ================================
# JWT 签名密钥 [必改]
# 至少 64 个字符的随机字符串
# 生成命令: openssl rand -base64 48
JWT_SECRET=easyssh-secret-change-in-production

# JWT 令牌过期时间（小时）
JWT_ACCESS_EXPIRE_HOURS=1
JWT_REFRESH_EXPIRE_HOURS=168

# 数据加密密钥 [必改]
# 必须是 32 字节（24 个 base64 字符）
# 生成命令: openssl rand -base64 24
ENCRYPTION_KEY=easyssh-encryption-key-32bytes!!

# ================================
# AI 配置（可选）
# ================================
# OpenAI API Key（用于 AI 功能）
OPENAI_API_KEY=your_openai_api_key_here
